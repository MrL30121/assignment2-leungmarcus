<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assignment 2</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex flex-column h-100 bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold text-info" href="/">Assignment 2</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="nav nav-pills ms-auto align-items-center">
    <li class="nav-item"><a href="/" class="nav-link <%= typeof navLink !== 'undefined' && navLink === 'home' ? 'active' : '' %>">Home</a></li>
    <li class="nav-item"><a href="/members" class="nav-link <%= typeof navLink !== 'undefined' && navLink === 'members' ? 'active' : '' %>">Members</a></li>
    <li class="nav-item"><a href="/admin" class="nav-link <%= typeof navLink !== 'undefined' && navLink === 'admin' ? 'active' : '' %>">Admin</a></li>
    <li class="nav-item"><a href="/login" class="nav-link <%= typeof navLink !== 'undefined' && navLink === 'login' ? 'active' : '' %>">Login</a></li>
    <li class="nav-item"><a href="/signup" class="nav-link <%= typeof navLink !== 'undefined' && navLink === 'signup' ? 'active' : '' %>">Sign Up</a></li>
</ul>
                </ul>
            </div>
        </div>
    </nav>